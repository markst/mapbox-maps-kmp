group=dev.mapboxkmp

# Gradle
org.gradle.jvmargs=-Xmx4096M -Dfile.encoding=UTF-8 -Dkotlin.daemon.jvm.options\="-Xmx2048M"
org.gradle.caching=true
org.gradle.configuration-cache=true
org.gradle.parallel=true

# Kotlin
kotlin.code.style=official
kotlin.mpp.enableCInteropCommonization=true
kotlin.apple.xcodeCompatibility.nowarn=true

# Android
android.useAndroidX=true
android.nonTransitiveRClass=true

# MAPBOX_DOWNLOADS_TOKEN and MAPBOX_PUBLIC_TOKEN are intentionally NOT set here.
# This file is tracked in git; secrets belong in your user-level
# ~/.gradle/gradle.properties (or an env var). See README "Credentials".
